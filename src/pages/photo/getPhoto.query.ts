import { UUID } from '../../domain'
import { PhotoPageProps } from './PhotoPage/PhotoPage'

export const getPhoto = async (chatId: UUID): Promise<PhotoPageProps> => {
  // TODO
  return {} as PhotoPageProps
}
